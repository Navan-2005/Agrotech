<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crop Information</title>
    <style>
        :root {
            --primary: #62bc6d;
            --primary-dark: #27ae60;
            --primary-light: #a8e6cf;
            --secondary: #34495e;
            --light: #f7f9fc;
            --dark: #2c3e50;
            --success: #28a745;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            background-color: var(--light);
            min-height: 100vh;
            padding: 2rem;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            text-align: center;
        }

        .get-info-btn {
            background-color: var(--primary);
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 8px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-bottom: 2rem;
        }

        .get-info-btn:hover {
            background-color: var(--primary-dark);
        }

        .info-container {
            background-color: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: left;
            display: none;
        }

        .info-container h2 {
            color: var(--secondary);
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--primary-light);
        }

        .info-content {
            color: var(--dark);
            line-height: 1.6;
            font-size: 1.1rem;
        }

        .loading {
            display: none;
            color: var(--secondary);
            font-size: 1.2rem;
            margin: 2rem 0;
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem;
            }
            
            .info-container {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <button class="get-info-btn" onclick="getInformation()">Get Information</button>
        
        <div id="loading" class="loading">
            Generating crop information...
        </div>

        <div id="infoContainer" class="info-container">
            <h2>Crop Information</h2>
            <div id="infoContent" class="info-content">
                <!-- Information will be inserted here -->
            </div>
        </div>
    </div>

    <script>
        function getInformation() {
            // Show loading state
            document.getElementById('loading').style.display = 'block';
            document.getElementById('infoContainer').style.display = 'none';

            // Simulate getting information (replace with your actual API call)
            setTimeout(() => {
                // Hide loading message
                document.getElementById('loading').style.display = 'none';
                
                // Show information container
                document.getElementById('infoContainer').style.display = 'block';
                
                // Update content (replace this with your actual model's output)
                document.getElementById('infoContent').innerHTML = `
                    <p>This is where your model's generated information about the crop will appear. The content can include:</p>
                    <ul style="margin: 1rem 0 1rem 2rem;">
                        <li>General description</li>
                        <li>Growing conditions</li>
                        <li>Care instructions</li>
                        <li>Common issues and solutions</li>
                        <li>Any other relevant information</li>
                    </ul>
                    <p>The information will be displayed in a clean, easy-to-read format when the user clicks the button.</p>
                `;
            }, 1500);
        } 
    </script>
</body>
</html>